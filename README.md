

# Learn Python

My Python learning projects, built while learning from scratch.

## Projects

- **notebook_oop.py** — A note-taking app built with OOP (classes), with JSON save/load so notes persist.
- **dictonary.py** — The earlier version of the notes app using functions and file storage.
- **validate.py** — Input validation practice (keeps asking until valid input).
- **todo.py** — A simple to-do list saver using file handling.

## What I learned

Variables, functions, loops, dictionaries, error handling, file handling, OOP, and JSON.

## How to run

```
python3 notebook_oop.py
```